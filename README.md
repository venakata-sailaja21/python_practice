# Python Practice

This repository contains my daily Python practice programs.

## Topics
- Variables
- Datatypes
- Typecasting
- Lists
- Strings
## VARIABLES 
- variables con't start with numbers and special characters 
- space should not be used when variables are created and it can't be used by in between words also.
- variables can't start with keywords
- variables are case sensitive
## DATATYPES
- there are 15 data types in the python
   - integer
   - float
   - boolean
   - complex
   - string
   - list 
   - tuple
   - dictionary
   - set
   - frozenset
   - memoryview
   - range
- Deaclare the variables and assign something like numbers, decimals, strings, characters.
- then use type() function to see which type of variables belongs to which data type.
- Firstly we are executing the datatypes like integer, float, boolean, string.
- We have inbuild functions like sum(),max(),min(),len(),type().
- This are used to do basic mathamatical operations on variables.
$$ Float $$
- In float 10e1 mean 10 is multiplied with 10.0, same as e2=100.0,e3=1000.0........  e indicate as 1 number indicates how many number of zeros should added to 1.

$$ String $$
- String is defined in single or double quotations.
- You should not use both quotations on single string python will catch error as syntaxerror.
    - Ex:- 'sailu"
- To highlight the text in a sentence use different quotes.
    - Ex:- "This is a 'Example'"